#include <stdio.h>

int main(void)
{
   /* char i;

    do
    {
        i = getchar();
        putchar(i);
    }
    while (i != '.')
        ;

    putchar('\n');
    */

    char first, second;
    while (first = getchar();)
        
   
    second = getchar();
    printf("1 - '%c', 2 - '%c'\n", first, second);
    return (0);
}