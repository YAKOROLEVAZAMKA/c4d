#include <stdio.h>

int main(void)
{
    //for (i = 0; i < lim-1 && (ñ = getchar()) != EOF && ñ != '\n'; ++i)
    //s[i] = c;
    int c;
    char s[];
    
    while (i < lim-1)
    {
        while (c = getchar() != EOF)
        {
            while (c != '\n')
            {
                i++;
                s[i] = c;
            }
        }
    }
}